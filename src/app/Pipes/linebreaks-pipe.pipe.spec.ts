import { LinebreaksPipePipe } from './linebreaks-pipe.pipe';

describe('LinebreaksPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LinebreaksPipePipe();
    expect(pipe).toBeTruthy();
  });
});
